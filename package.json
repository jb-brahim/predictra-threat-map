{
  "name": "predictra-threat-map",
  "version": "1.0.0",
  "description": "Predictra Threat Map with React Frontend and Node.js Backend",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:backend --prefix frontend",
    "start:backend": "node backend/server.js",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run start:backend\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}